// Shared Providers — Injeção de dependências via Context API
export { DatabaseProvider, useDatabase } from './DatabaseProvider';
export { useNotification } from './NotificationProvider';
export { SharedThemeProvider, useAppTheme } from './ThemeProvider';
