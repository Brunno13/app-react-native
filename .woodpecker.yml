when:
  - event: push
    branch: main

steps:
  install-and-check:
    image: oven/bun:latest
    commands:
      - echo "📦 Instalando dependências com Bun..."
      - bun install
      - echo "🔍 Verificando tipagem TypeScript..."
      - bunx tsc --noEmit