pipeline:
  install-and-check:
    image: oven/bun:latest
    commands:
      - echo "📦 Instalando dependências com Bun..."
      - bun install
      - echo "🔍 Verificando tipagem TypeScript..."
      - bunx tsc --noEmit
      # Se você adicionar ESLint depois, pode rodar: bun run lint