when:
  - event: push
    branch: main
  - event: tag

clone:
  git:
    image: woodpeckerci/plugin-git
    extra_hosts:
      - "gitea.brunnoserver.duckdns.org:192.168.31.215"

steps:
  # =========================================================
  # FASE 1: Verificação (Apenas no Push da Main) 
  # =========================================================
  - name: testar-codigo
    image: oven/bun:latest
    commands:
      - echo "📦 Instalando dependências..."
      - bun install
      - echo "🔍 Verificando tipagem do App (React Native)..."
      - bunx tsc --noEmit
      - echo "🔍 Verificando tipagem dos Scripts de Automação (Bun)..."
      - bunx tsc --project scripts/tsconfig.json --noEmit
    when:
      - event: push
        branch: main

  # =========================================================
  # FASE 2: Build do Android (Apenas na Tag)
  # =========================================================
  - name: build-apk
    image: ghcr.io/cirruslabs/android-sdk:34
    commands:
      # -----------------------------------------------------------------
      # PREPARANDO O AMBIENTE NATIVO ARM64 (Orange Pi)
      # -----------------------------------------------------------------
      - echo "🔧 Atualizando repositórios e instalando dependências base..."
      - apt-get update && apt-get install -y curl ninja-build clang lld llvm
      
      - echo "🔧 Instalando Node.js 20..."
      - curl -fsSL https://deb.nodesource.com/setup_20.x | bash -
      - apt-get install -y nodejs
      
      - echo "🔗 Instalando pacote completo do CMake 3.22.1 Oficial ARM64..."
      - rm -rf /opt/android-sdk-linux/cmake/3.22.1
      - mkdir -p /opt/android-sdk-linux/cmake/3.22.1
      - curl -L https://github.com/Kitware/CMake/releases/download/v3.22.1/cmake-3.22.1-linux-aarch64.tar.gz | tar -xz -C /opt/android-sdk-linux/cmake/3.22.1 --strip-components=1
      - ln -sf /usr/bin/ninja /opt/android-sdk-linux/cmake/3.22.1/bin/ninja

      - echo "📦 Baixando NDK 27.1.12297006 exigido pelo Expo..."
      - yes | sdkmanager "ndk;27.1.12297006"
      
      - echo "🔗 Fazendo transplante do compilador C++ (Clang) para ARM64..."
      - export NDK_BIN=/opt/android-sdk-linux/ndk/27.1.12297006/toolchains/llvm/prebuilt/linux-x86_64/bin
      - rm -f $NDK_BIN/clang $NDK_BIN/clang++ $NDK_BIN/ld.lld $NDK_BIN/llvm-ar $NDK_BIN/llvm-strip $NDK_BIN/llvm-objcopy $NDK_BIN/llvm-readelf
      - ln -sf /usr/bin/clang $NDK_BIN/clang
      - ln -sf /usr/bin/clang++ $NDK_BIN/clang++
      - ln -sf /usr/bin/ld.lld $NDK_BIN/ld.lld
      - ln -sf /usr/bin/llvm-ar $NDK_BIN/llvm-ar
      - ln -sf /usr/bin/llvm-strip $NDK_BIN/llvm-strip
      - ln -sf /usr/bin/llvm-objcopy $NDK_BIN/llvm-objcopy
      - ln -sf /usr/bin/llvm-readelf $NDK_BIN/llvm-readelf
      # -----------------------------------------------------------------
      
      - echo "🔧 Instalando o Bun no contêiner Android..."
      - curl -fsSL https://bun.sh/install | bash
      - export PATH="$HOME/.bun/bin:$PATH"
      
      - echo "📦 Instalando dependências do projeto..."
      - bun install
      
      - echo "🚀 Executando o script Bun-Native de build (Produção)..."
      - bun run build:apk:prod
    when:
      - event: tag

  # =========================================================
  # FASE 3: Release no Gitea Interno (Apenas na Tag)
  # =========================================================
  - name: gerar-release-notes
    image: alpine
    environment:
      GITEA_TOKEN:
        from_secret: gitea_token
    commands:
      - apk add --no-cache curl git jq
      - |
        set -e
        set -x
        
        git config remote.origin.fetch "+refs/heads/*:refs/remotes/origin/*"
        git fetch --all --unshallow || git fetch --all
        git fetch --tags
        
        PREV_TAG=$(git describe --tags --abbrev=0 HEAD^ 2>/dev/null || echo "")
        
        if [ -z "$PREV_TAG" ]; then
          CHANGELOG=$(git log --pretty=format:"- %h %s")
        else
          CHANGELOG=$(git log --pretty=format:"- %h %s" $PREV_TAG..HEAD)
        fi
        
        RELEASE_BODY="🚀 Deploy automático do APK realizado com sucesso pelo Woodpecker CI. 
        
        ---
        
        $CHANGELOG"
        
        echo "$RELEASE_BODY" > .release_body.txt
        
        JSON_PAYLOAD=$(jq -n --arg tag "${CI_COMMIT_TAG}" --arg name "Versão ${CI_COMMIT_TAG}" --arg body "$RELEASE_BODY" '{tag_name: $tag, name: $name, body: $body}')
        
        echo "=== ENVIANDO RELEASE PARA O GITEA ==="
        RESPONSE=$(curl -s -X POST "http://192.168.31.215:3099/api/v1/repos/${CI_REPO_OWNER}/${CI_REPO_NAME}/releases" -H "accept: application/json" -H "Authorization: token $GITEA_TOKEN" -H "Content-Type: application/json" -d "$JSON_PAYLOAD")
        
        echo "Resposta da API do Gitea: $RESPONSE"
        RELEASE_ID=$(echo "$RESPONSE" | jq -r '.id')
        
        if [ "$RELEASE_ID" == "null" ] || [ -z "$RELEASE_ID" ]; then
          echo "❌ ERRO: O Gitea não retornou um ID válido. Se a Tag já existir, apague-a no painel e tente novamente."
          exit 1
        fi
        
        echo "=== INICIANDO UPLOAD DO APK ==="
        # Realiza o upload do APK gerado na FASE 2
        curl -f -s -X POST "http://192.168.31.215:3099/api/v1/repos/${CI_REPO_OWNER}/${CI_REPO_NAME}/releases/$RELEASE_ID/assets?name=app-react-native-production.apk" -H "Authorization: token $GITEA_TOKEN" -F "attachment=@./app-react-native-production.apk"
        
        echo "✅ DEPLOY FINALIZADO COM SUCESSO NO GITEA!"
    when:
      - event: tag

  # =========================================================
  # FASE 4: Sincronização com GitHub (Apenas na Tag)
  # =========================================================
  #- name: espelhar-github
  #  image: alpine/git
  #  environment:
  #    GITHUB_USER: Brunno13
  #    GITHUB_TOKEN:
  #      from_secret: github_token
  #  commands:
  #    - echo "=== SINCRONIZANDO COM O GITHUB ==="
  #    - git config --global --add safe.directory '*'
  #    # Repositório alterado de api-bun para app-react-native
  #    - git remote add github https://$GITHUB_USER:$GITHUB_TOKEN@github.com/$GITHUB_USER/app-react-native.git
  #    - git push github main --force
  #    - git push github ${CI_COMMIT_TAG} --force
  #  when:
  #    - event: tag
#
  #- name: criar-release-github
  #  image: alpine
  #  environment:
  #    GITHUB_USER: Brunno13
  #    GITHUB_TOKEN:
  #      from_secret: github_token
  #  commands:
  #    - apk add --no-cache curl jq
  #    - |
  #      set -e
  #      echo "=== CRIANDO RELEASE NO GITHUB ==="
  #      # Endpoint atualizado para o repositório do React Native
  #      GITHUB_API_URL="https://api.github.com/repos/$GITHUB_USER/app-react-native/releases"
  #      
  #      if [ -f .release_body.txt ]; then
  #        RELEASE_BODY=$(cat .release_body.txt)
  #      else
  #        RELEASE_BODY="🚀 Lançamento automatizado do App sincronizado a partir do servidor Gitea interno."
  #      fi
  #      
  #      JSON_PAYLOAD=$(jq -n --arg tag "${CI_COMMIT_TAG}" --arg name "Versão ${CI_COMMIT_TAG}" --arg body "$RELEASE_BODY" '{tag_name: $tag, name: $name, body: $body}')
  #      
  #      RESPONSE=$(curl -s -X POST $GITHUB_API_URL -H "Accept: application/vnd.github+json" -H "Authorization: Bearer $GITHUB_TOKEN" -d "$JSON_PAYLOAD")
  #      RELEASE_ID=$(echo "$RESPONSE" | jq -r '.id')
  #      
  #      if [ "$RELEASE_ID" == "null" ] || [ -z "$RELEASE_ID" ]; then
  #        echo "❌ ERRO: O GitHub não retornou um ID válido. Resposta: $RESPONSE"
  #        exit 1
  #      fi
  #      
  #      echo "=== INICIANDO UPLOAD DO APK NO GITHUB ==="
  #      # URL de upload atualizada para o repositório do app
  #      GH_UPLOAD_URL="https://uploads.github.com/repos/$GITHUB_USER/app-react-native/releases/$RELEASE_ID/assets"
  #      
  #      # Faz o upload binário do APK gerado pelo Bun
  #      curl -f -s -X POST "$GH_UPLOAD_URL?name=app-react-native-production.apk" -H "Authorization: Bearer $GITHUB_TOKEN" -H "Content-Type: application/vnd.android.package-archive" --data-binary @"./app-react-native-production.apk"
  #      
  #      echo "✅ RELEASE NO GITHUB CRIADA COM SUCESSO!"
  #  when:
  #    - event: tag